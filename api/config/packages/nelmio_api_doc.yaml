nelmio_api_doc:
    documentation:
        info:
            title: Online Exams
            description: Create and publish online tests
            version: 1.0.0
#        securityDefinitions:
#            Bearer:
#                type: apiKey
#                description: 'Value: Bearer {jwt}'
#                name: Authorization
#                in: header
#        security:
#            - Bearer: []
    areas:
        default:
            path_patterns:
                - ^/api/v1(?!/doc$)
        test:
            path_patterns: [ ^/api/v1/test ]
        answer:
            path_patterns: [ ^/api/v1/answer ]

nelmio_api_doc:
    documentation:
        info:
            title: Online Exams
            description: Create and publish online tests
            version: 1.0.0
#        securityDefinitions:
#            Bearer:
#                type: apiKey
#                description: 'Value: Bearer {jwt}'
#                name: Authorization
#                in: header
#        security:
#            - Bearer: []
    areas: # to filter documented areas
        path_patterns:
            - ^/api/v1/(?!/doc$) # Accepts routes under /api/v1 except /api/v1/doc
